# X 项目监控巡检系统

## 麒麟系统快速安装部署文档

> 适用于银河麒麟 V10 操作系统 | 面向零基础运维人员

---

## 一、项目简介

X 项目是一个**自动化监控巡检系统**，可以：

1. **监控服务器状态** - CPU、内存、磁盘、网络等
2. **收集日志** - 从 Elasticsearch 收集系统日志
3. **AI 分析** - 使用 Claude AI 分析监控数据，发现问题
4. **自动报告** - 生成巡检报告，包含异常告警和处理建议

---

## 二、环境要求

### 2.1 硬件要求

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4 核 | 8 核 |
| 内存 | 8 GB | 16 GB |
| 磁盘 | 100 GB | 200 GB |

### 2.2 软件要求

- 操作系统：银河麒麟 V10 SP1/SP2（x86_64）
- Python：3.10+

---

## 三、安装步骤

本项目支持两种运行方式，请根据情况选择：

- **方式一：Docker 部署** - 一键启动所有监控服务
- **方式二：Python 直接运行** - 只运行巡检脚本，连接远程服务

---

## 四、Docker 部署（方式一）

### 4.1 安装 Docker

> ⚠️ 如果 Docker 已安装，请跳过此步骤

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 将用户加入 docker 组
sudo usermod -aG docker $USER
```

> ⚠️ 执行后需要**重启终端**或**重新登录**才能生效

### 4.2 下载项目代码

```bash
cd ~
git clone https://github.com/liuliu4356/kzx.git
cd kzx
```

> 💡 如果 Git 克隆失败，尝试：
> ```bash
> git clone https://ghproxy.com/https://github.com/liuliu4356/kzx.git
> ```

### 4.3 启动服务

```bash
cd ~/kzx
docker-compose up -d
```

等待 2-3 分钟，查看状态：

```bash
docker-compose ps
```

### 4.4 验证服务

| 服务 | 地址 | 用户名 | 密码 |
|------|------|--------|------|
| Grafana | http://localhost:3000 | admin | admin |
| Prometheus | http://localhost:9090 | - | - |
| Elasticsearch | http://localhost:9200 | - | - |

---

## 五、Python 直接运行（方式二）

如果不方便使用 Docker，可以使用此方式。只运行巡检脚本，连接远程服务。

### 5.1 安装 Python

```bash
sudo apt-get install -y python3 python3-pip python3-venv
```

检查版本：

```bash
python3 --version
```

### 5.2 下载项目代码

```bash
cd ~
git clone https://github.com/liuliu4356/kzx.git
cd kzx
```

### 5.3 创建虚拟环境

```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 5.4 安装依赖

```bash
pip install -r requirements.txt
```

> 💡 如果安装慢，使用国内源：
> ```bash
> pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 5.5 配置 API Key

创建 `.env` 文件：

```bash
vi .env
```

添加以下内容：

```
ANTHROPIC_API_KEY=你的API密钥
```

> ⚠️ 需要去 https://www.anthropic.com/ 申请 API Key

### 5.6 修改配置文件

```bash
vi config.yaml
```

修改 prometheus 和 elasticsearch 地址：

```yaml
prometheus:
  url: http://实际服务器IP:9090

elasticsearch:
  url: http://实际服务器IP:9200
```

### 5.7 运行巡检

```bash
# 激活虚拟环境
source .venv/bin/activate

# 运行巡检（跳过 AI 分析）
python -m src.main inspect --skip-llm

# 或者运行完整巡检（含 AI 分析）
python -m src.main inspect
```

### 5.8 查看报告

```bash
ls -la reports/
cat reports/最新报告文件名.md
```

---

## 六、常用命令

### 6.1 Docker 命令

| 命令 | 说明 |
|------|------|
| `docker-compose up -d` | 启动所有服务 |
| `docker-compose down` | 停止所有服务 |
| `docker-compose ps` | 查看服务状态 |
| `docker-compose logs -f` | 查看日志 |

### 6.2 巡检命令

| 命令 | 说明 |
|------|------|
| `python -m src.main inspect` | 运行巡检（含 AI） |
| `python -m src.main inspect --skip-llm` | 运行巡检（跳过 AI） |
| `python -m src.main web` | 启动 Web 界面 |

---

## 七、常见问题

### Q1: Docker 启动失败

**可能原因：** 端口被占用

**解决方法：** 检查端口占用或修改 docker-compose.yml 中的端口

### Q2: 巡检报告生成失败

**可能原因：** 
1. API Key 未设置
2. Prometheus 无数据

**解决方法：**

```bash
# 检查 API Key
echo $ANTHROPIC_API_KEY

# 检查 Prometheus
curl http://localhost:9090/api/v1/query?query=up
```

### Q3: Python 依赖安装失败

**解决方法：**

```bash
# 使用国内源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 八、验证部署

```bash
# 查看服务状态
docker-compose ps

# 运行巡检
python -m src.main inspect --skip-llm

# 查看报告
ls reports/
```

---

## 九、技术支持

- 项目地址：https://github.com/liuliu4356/kzx

---

**文档版本：** v1.0  
**更新日期：** 2026-05-06