# X 项目使用说明

> 此文档每 10 分钟自动更新 | 更新时间: 2026-05-02 16:36

---

## 一、项目是什么？

这是一个**自动化监控巡检系统**，可以帮助你：

1. **监控服务器状态** - CPU、内存、磁盘使用率
2. **收集日志** - 收集并分析系统日志
3. **AI 分析** - 使用 Claude AI 分析监控数据，发现问题
4. **自动报告** - 生成巡检报告，包含问题和建议

---

## 二、快速开始

### 步骤 1: 启动所有监控服务

```bash
cd D:\claude_code开发\X
.\start.bat
```

或使用 Docker 命令：

```bash
docker compose up -d
```

### 步骤 2: 等待服务启动（约 1-2 分钟）

访问以下地址确认服务正常：

| 服务 | 地址 | 说明 |
|------|------|------|
| Prometheus | http://localhost:9090 | 监控指标查看 |
| Grafana | http://localhost:3000 | 可视化面板 (账密: admin/admin) |
| Elasticsearch | http://localhost:9200 | 日志存储 |
| Kibana | http://localhost:5601 | 日志分析 |
| Node Exporter | http://localhost:9100 | 系统指标采集 |
| Logstash | http://localhost:9600 | 日志处理 |

### 步骤 3: 运行巡检

```bash
cd D:\claude_code开发\X

# 激活虚拟环境
.venv\Scripts\activate

# 运行巡检
python -m src.main inspect

# 或者跳过 AI 分析（更快）
python -m src.main inspect --skip-llm
```

### 步骤 4: 查看报告

报告生成在 `reports/` 目录，文件名格式：`YYYY-MM-DD-HHMM.md`

---

## 三、配置说明

### 配置文件位置

- 主配置: `config.yaml`
- 示例配置: `config.example.yaml`

### 关键配置项

```yaml
prometheus:
  url: http://localhost:9090      # Prometheus 地址

elasticsearch:
  url: http://localhost:9200    # Elasticsearch 地址

llm:
  provider: anthropic            # AI 提供商
  model: claude-sonnet-4-6      # 使用的模型
  # 需要设置环境变量: ANTHROPIC_API_KEY

report:
  output_dir: reports           # 报告输出目录
  language: zh-CN               # 报告语言
```

### 设置 API Key

创建 `.env` 文件：

```
ANTHROPIC_API_KEY=你的API密钥
```

---

## 四、命令参考

| 命令 | 说明 |
|------|------|
| `python -m src.main inspect` | 运行完整巡检 |
| `python -m src.main inspect --skip-llm` | 运行巡检（跳过 AI） |
| `python -m src.main inspect --output-dir myreports` | 指定报告目录 |
| `python -m src.main init-config` | 初始化配置文件 |

---

## 五、当前状态

### 运行中的容器

```
prometheus      - 监控指标存储
node-exporter   - 系统指标采集
alertmanager    - 告警管理
grafana         - 可视化面板
elasticsearch   - 日志存储
kibana          - 日志分析
logstash        - 日志处理
filebeat        - 日志收集
```

### 端口映射

| 容器 | 端口 |
|------|------|
| prometheus | 9090 |
| grafana | 3000 |
| elasticsearch | 9200 |
| kibana | 5601 |
| node-exporter | 9100 |
| logstash | 9600, 8080, 5044 |
| alertmanager | 9093 |

---

## 六、常见问题

### Q: 服务启动失败？
A: 确保 Docker Desktop 已启动，运行 `docker ps` 检查容器状态

### Q: 报告生成失败？
A: 检查 `.env` 文件中是否设置了 `ANTHROPIC_API_KEY`

### Q: Prometheus 没数据？
A: 访问 http://localhost:9090 -> Status -> Targets 查看采集状态

---

## 七、停止服务

```bash
cd D:\claude_code开发\X
docker compose down
```

或运行 `stop.bat`

---

## 八、目录结构

```
X/
├── src/                    # Python 源代码
│   ├── main.py            # 主入口
│   ├── config.py          # 配置加载
│   ├── analyzer.py       # AI 分析
│   ├── collectors/        # 数据采集
│   └── reporters/        # 报告生成
├── docker-compose.yml     # Docker 编排
├── config.yaml           # 主配置
├── reports/              # 巡检报告输出
├── prometheus/           # Prometheus 配置
├── grafana/               # Grafana 配置
├── elasticsearch/         # ES 数据目录
└── logstash/             # Logstash 配置
```