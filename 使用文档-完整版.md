# X项目监控系统 - 完整使用文档

> 文档版本: v1.2.0
> 更新日期: 2026-05-02
> 面向对象: 运维小白

---

## 目录

1. [项目概述](#1-项目概述)
2. [环境要求](#2-环境要求)
3. [快速开始(测试环境)](#3-快速开始测试环境)
4. [生产环境部署](#4-生产环境部署)
5. [功能验证指南](#5-功能验证指南)
6. [物理机模拟测试方法](#6-物理机模拟测试方法)
7. [配置详解](#7-配置详解)
8. [常见问题排查](#8-常见问题排查)
9. [维护与更新](#9-维护与更新)

---

## 1. 项目概述

### 1.1 什么是X项目监控系统？

X项目是一套**自动化运维巡检系统**，用于监控多机房（数据中心）的服务器和应用健康状态。

**核心功能：**
- ✅ 自动采集服务器指标（CPU、内存、磁盘、网络）
- ✅ 自动采集数据库性能数据（连接数、QPS、TPS）
- ✅ 自动采集应用日志（错误、警告、慢查询）
- ✅ 智能阈值判断，异常自动标记
- ✅ 生成巡检报告（支持Markdown格式）
- ✅ 支持AI智能分析（可选）

### 1.2 支持的机房

| 机房名称 | 代码 | 用途 |
|----------|------|------|
| 北京东坝 | dongba | 主机房 |
| 北京南法信 | nanfaxin | 主机房 |
| 合肥 | hefei | 灾备机房 |

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    X 巡检系统                            │
├─────────────────────────────────────────────────────────┤
│  数据采集层                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  Prometheus │  │Elasticsearch│  │  定时任务   │   │
│  │   (指标采集) │  │  (日志采集)  │  │ (自动执行)  │   │
│  └─────────────┘  └─────────────┘  └─────────────┘   │
├─────────────────────────────────────────────────────────┤
│  数据存储层                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  Prometheus │  │Elasticsearch│  │   Grafana   │   │
│  │   (时序数据) │  │  (日志数据)  │  │  (可视化)   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘   │
├─────────────────────────────────────────────────────────┤
│  应用层                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │   巡检程序  │  │   报告生成   │  │   AI分析    │   │
│  │  (Python)   │  │  (Markdown)  │  │ (可选)       │   │
│  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 环境要求

### 2.1 最低配置

| 组件 | CPU | 内存 | 磁盘 | 说明 |
|------|-----|------|------|------|
| Prometheus | 2核 | 4GB | 50GB | 指标存储 |
| Elasticsearch | 2核 | 4GB | 100GB | 日志存储 |
| Grafana | 1核 | 1GB | 10GB | 可视化 |
| 巡检程序 | 1核 | 1GB | 10GB | Python应用 |

### 2.2 软件依赖

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Docker | 20.10+ | 容器编排 |
| Docker Compose | 2.0+ | 容器编排 |
| Python | 3.10+ | 运行巡检程序 |
| 网络要求 | 互通 | 所有组件网络互通 |

### 2.3 支持的操作系统

- ✅ Ubuntu 20.04+
- ✅ CentOS 7+
- ✅ 麒麟V10（鲲鹏/飞腾）
- ✅ Windows Server 2019+

---

## 3. 快速开始(测试环境)

> 本章节介绍如何在30分钟内快速搭建测试环境

### 3.1 一键启动（推荐）

```bash
# 1. 克隆项目
git clone https://github.com/liuliu4356/kzx.git
cd kzx

# 2. 一键启动所有服务
docker-compose up -d

# 3. 检查服务状态
docker-compose ps
```

**预期输出：**
```
     Name                    Command               State    Ports
---------------------------------------------------------------------------
prometheus          /bin/prometheus --config ...   Up      9090/tcp
elasticsearch      /usr/local/bin/docker-ent ...   Up      9200/tcp
grafana            /run.sh                         Up      3000/tcp
...
```

### 3.2 访问服务

| 服务 | 地址 | 用户名 | 密码 |
|------|------|--------|------|
| Grafana | http://localhost:3000 | admin | admin |
| Prometheus | http://localhost:9090 | - | - |
| Elasticsearch | http://localhost:9200 | - | - |

### 3.3 运行第一次巡检

```bash
# 进入容器运行巡检
docker exec -it kzx python -m src.main inspect --skip-llm

# 查看报告
ls -la reports/
```

---

## 4. 生产环境部署

### 4.1 部署架构选择

#### 方案A：单节点部署（适用于小型环境）
- 所有组件部署在一台服务器
- 优点：简单、成本低
- 缺点：单点故障、扩展性差

#### 方案B：集群部署（推荐生产环境）
- Prometheus集群（3节点）
- Elasticsearch集群（3节点）
- 优点：高可用、可扩展
- 缺点：成本较高

### 4.2 生产环境部署步骤

#### 第一步：服务器准备

```bash
# 1. 创建专用用户（安全）
useradd -m -s /bin/bash xmonitor
passwd xmonitor  # 设置密码

# 2. 安装Docker
curl -fsSL https://get.docker.com | sh
systemctl enable docker
systemctl start docker

# 3. 安装Docker Compose
curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

#### 第二步：网络规划

```bash
# 创建专用网络
docker network create --subnet=172.20.0.0/16 x-network
```

#### 第三步：配置修改

```bash
# 1. 复制配置
cp config.example.yaml config.yaml

# 2. 修改Prometheus地址（生产环境实际IP）
vi config.yaml
# 将 http://prometheus:9090 改为实际IP地址
```

#### 第四步：数据持久化

```yaml
# docker-compose.yml 中添加卷挂载
services:
  prometheus:
    volumes:
      - ./prometheus-data:/prometheus
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml

  elasticsearch:
    volumes:
      - ./es-data:/usr/share/elasticsearch/data
```

#### 第五步：资源限制

```yaml
# 生产环境建议添加资源限制
services:
  prometheus:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
```

#### 第六步：安全加固

```bash
# 1. 配置防火墙
firewall-cmd --permanent --add-port=9090/tcp   # Prometheus
firewall-cmd --permanent --add-port=3000/tcp   # Grafana
firewall-cmd --permanent --add-port=9200/tcp   # ES
firewall-cmd --reload

# 2. 配置HTTPS（生产环境必须）
# 见 7.4 节 HTTPS配置
```

### 4.3 生产环境检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Docker版本 | [ ] | 20.10+ |
| 内存充足 | [ ] | 至少16GB |
| 磁盘充足 | [ ] | 至少200GB |
| 网络互通 | [ ] | 所有组件能通信 |
| 防火墙配置 | [ ] | 开放必要端口 |
| 数据备份 | [ ] | 配置数据持久化 |
| 监控告警 | [ ] | 配置邮件/钉钉告警 |

---

## 5. 功能验证指南

> 每个功能模块的验证方法，确保系统正常工作

### 5.1 Prometheus 指标采集验证

#### 验证目标
确认Prometheus能正确采集服务器指标数据

#### 验证方法

```bash
# 方法1：通过Web界面验证
# 1. 打开 http://localhost:9090
# 2. 在查询框输入: up
# 3. 点击执行，应该看到类似：
# up{job="prometheus",instance="localhost:9090"} 1

# 方法2：通过命令行验证
docker exec prometheus promtool check config /etc/prometheus/prometheus.yml
```

#### 预期结果
- ✅ 能查询到 `up` 指标，值为 1
- ✅ 能查询到 `node_load1` 指标
- ✅ 能查询到 `node_memory_*` 指标

#### 验证不通过怎么办？
- 检查Prometheus容器是否运行: `docker ps | grep prometheus`
- 检查日志: `docker logs prometheus`
- 检查网络: `docker network ls`

---

### 5.2 异常阈值检测验证

#### 验证目标
确认系统能正确识别指标异常

#### 验证方法

```bash
# 1. 人为制造异常（模拟）
# 方法A：在Prometheus中添加异常数据（见5.2.1）
# 方法B：修改阈值使正常值变为异常

# 2. 运行巡检
python -m src.main inspect --skip-llm

# 3. 查看报告中的异常标记
grep -A5 "异常" reports/*.md
```

#### 预期结果
- ✅ 异常的指标显示 `⚠️ 异常`
- ✅ 正常的指标显示 `✅ 正常`

#### 5.2.1 模拟异常指标（测试用）

```python
# 在Prometheus中添加测试异常数据
# 1. 创建mock服务（见第6章）
# 2. 在prometheus.yml中添加job:
  - job_name: 'test-anomaly'
    static_configs:
      - targets: ['mock-metrics:9100']
```

---

### 5.3 Elasticsearch 日志采集验证

#### 验证目标
确认ES能正确接收和查询日志

#### 验证方法

```bash
# 方法1：通过Kibana验证
# 1. 打开 http://localhost:5601
# 2. 进入Dev Tools
# 3. 执行:
GET /logstash-*/_search
{
  "query": {
    "match_all": {}
  },
  "size": 10
}

# 方法2：通过命令行验证
curl 'http://localhost:9200/logstash-*/_count'
```

#### 预期结果
- ✅ 能查询到日志数据
- ✅ 能按level筛选（ERROR、WARN）
- ✅ 能按message关键字搜索

---

### 5.4 日志异常检测验证

#### 验证目标
确认巡检能检测到ERROR、WARN等异常日志

#### 验证方法

```bash
# 1. 添加测试日志到ES（见5.4.1）
# 2. 运行巡检
python -m src.main inspect --skip-llm
# 3. 查看报告中日志部分
```

#### 预期结果
- ✅ ERROR日志显示命中数量
- ✅ WARN日志显示命中数量
- ✅ 展开能看到具体日志内容

#### 5.4.1 模拟异常日志

```python
# 向ES添加测试日志
import requests
import json

logs = [
    {"@timestamp": "2026-05-02T10:00:00Z", "level": "ERROR", "message": "Database connection failed"},
    {"@timestamp": "2026-05-02T10:01:00Z", "level": "WARN", "message": "High memory usage 85%"},
    {"@timestamp": "2026-05-02T10:02:00Z", "level": "INFO", "message": "Slow query detected took 5s"},
]

bulk = ""
for log in logs:
    bulk += json.dumps({"index": {"_index": "logstash-2026.05.02"}}) + "\n"
    bulk += json.dumps(log) + "\n"

requests.post("http://localhost:9200/_bulk",
              data=bulk.encode(),
              headers={"Content-Type": "application/x-ndjson"})
```

---

### 5.5 巡检报告生成验证

#### 验证目标
确认能生成完整的巡检报告

#### 验证方法

```bash
# 运行巡检
python -m src.main inspect --skip-llm

# 查看报告
ls -la reports/
cat reports/$(ls -t reports/ | head -1)
```

#### 报告应包含内容
- ✅ 巡检时间、机房数
- ✅ Prometheus指标汇总表
- ✅ ES日志汇总表
- ✅ 异常指标标记
- ✅ 建议（如配置）

---

### 5.6 定时自动巡检验证

#### 验证目标
确认能按设定时间自动运行巡检

#### 验证方法

```bash
# 方法1：Linux定时任务
crontab -e
# 添加: */30 * * * * /path/to/inspect.sh

# 方法2：Windows计划任务
# 打开"任务计划程序" -> 创建基本任务
```

#### 预期结果
- ✅ 每30分钟自动运行一次
- ✅ 自动生成报告
- ✅ 可推送Git（可选）

---

### 5.7 GitHub自动同步验证

#### 验证目标
确认代码能自动推送到GitHub

#### 验证方法

```bash
# 1. 配置Git
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

# 2. 运行同步脚本
python auto_push.py

# 3. 查看GitHub仓库
# 访问 https://github.com/liuliu4356/kzx
```

#### 预期结果
- ✅ 本地代码推送到GitHub
- ✅ 自动创建版本标签

---

## 6. 物理机模拟测试方法

> 在没有Docker的环境下，如何模拟测试（适用于麒麟系统、物理机）

### 6.1 环境说明

**不使用Docker的场景：**
- 麒麟系统没有Docker
- 测试物理机环境
- 学习验证原理

### 6.2 Prometheus 物理机安装

#### 6.2.1 下载安装

```bash
# 1. 下载Prometheus（麒麟系统根据架构选择）
# x86_64架构:
wget https://github.com/prometheus/prometheus/releases/download/v2.51.0/prometheus-2.51.0.linux-amd64.tar.gz
# 鲲鹏(arm64)架构:
wget https://github.com/prometheus/prometheus/releases/download/v2.51.0/prometheus-2.51.0.linux-arm64.tar.gz

# 2. 解压
tar xzf prometheus-2.51.0.linux-amd64.tar.gz
cd prometheus-2.51.0.linux-amd64

# 3. 创建目录
mkdir -p /data/prometheus
mkdir -p /etc/prometheus

# 4. 复制配置文件
cp prometheus.yml /etc/prometheus/

# 5. 创建启动用户（安全）
useradd -r -s /sbin/nologin prometheus
chown -R prometheus:prometheus /data/prometheus
```

#### 6.2.2 配置systemd服务

```bash
# 创建服务文件
cat > /etc/systemd/system/prometheus.service << EOF
[Unit]
Description=Prometheus Monitoring
After=network.target

[Service]
Type=simple
User=prometheus
ExecStart=/opt/prometheus/prometheus \\
    --config.file=/etc/prometheus/prometheus.yml \\
    --storage.tsdb.path=/data/prometheus \\
    --web.console.libraries=/opt/prometheus/consoles \\
    --web.console.templates=/opt/prometheus/console_libraries \\
    --web.listen-address=:9090
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 启动服务
systemctl daemon-reload
systemctl enable prometheus
systemctl start prometheus

# 检查状态
systemctl status prometheus
```

#### 6.2.3 验证安装

```bash
# 访问Web界面
curl http://localhost:9090/-/healthy
# 输出: Prometheus is Healthy.

# 查询指标
curl 'http://localhost:9090/api/v1/query?query=up'
```

### 6.3 Node Exporter 物理机安装

#### 6.3.1 安装步骤

```bash
# 1. 下载（在被监控服务器上）
wget https://github.com/prometheus/node_exporter/releases/download/v1.7.0/node_exporter-1.7.0.linux-amd64.tar.gz

# 2. 解压并安装
tar xzf node_exporter-1.7.0.linux-amd64.tar.gz
cp node_exporter-1.7.0.linux-amd64/node_exporter /usr/local/bin/

# 3. 创建systemd服务
cat > /etc/systemd/system/node-exporter.service << EOF
[Unit]
Description=Node Exporter
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/node_exporter --collector.systemd --collector.cpu --collector.meminfo
Restart=always

[Install]
WantedBy=multi-user.target
EOF

# 4. 启动
systemctl daemon-reload
systemctl enable node-exporter
systemctl start node-exporter
```

#### 6.3.2 验证

```bash
# 检查端口
curl http://localhost:9100/metrics | head -20
```

### 6.4 Elasticsearch 物理机安装

#### 6.4.1 安装Java环境

```bash
# 麒麟系统可能需要先安装JDK
# 方法1：使用内置JDK
# 方法2：安装OpenJDK
yum install -y java-11-openjdk java-11-openjdk-devel
# 或
apt install -y openjdk-11-jdk
```

#### 6.4.2 安装ES

```bash
# 1. 下载ES（注意选择正确架构）
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-8.13.0-linux-x86_64.tar.gz

# 2. 解压
tar xzf elasticsearch-8.13.0-linux-x86_64.tar.gz
mv elasticsearch-8.13.0 /opt/elasticsearch

# 3. 创建用户
useradd -r -s /sbin/nologin elasticsearch
chown -R elasticsearch:elasticsearch /opt/elasticsearch

# 4. 配置JVM内存（根据服务器配置调整）
vi /opt/elasticsearch/config/jvm.options
# 修改: -Xms2g -Xmx2g 为实际内存的一半
```

#### 6.4.3 配置ES

```bash
# 修改配置
vi /opt/elasticsearch/config/elasticsearch.yml

# 添加以下配置（单机模式）:
cluster.name: x-monitor
node.name: node-1
path.data: /data/elasticsearch
path.logs: /var/log/elasticsearch
network.host: 0.0.0.0
http.port: 9200
discovery.type: single-node
```

#### 6.4.4 启动ES

```bash
# 启动
su - elasticsearch -c "/opt/elasticsearch/bin/elasticsearch -d"

# 检查
curl http://localhost:9200
```

### 6.5 Grafana 物理机安装

```bash
# 1. 添加yum源（CentOS/RHEL）
yum install -y https://dl.grafana.com/oss/release/grafana-10.3.0-1.x86_64.rpm

# 或 apt（Debian/Ubuntu）
apt-get install -y grafana

# 2. 配置
vi /etc/grafana/grafana.ini
# 修改:
# http_port = 3000
# admin_user = admin
# admin_password = admin

# 3. 启动
systemctl enable grafana-server
systemctl start grafana-server

# 4. 访问
# 浏览器打开 http://服务器IP:3000
```

### 6.6 巡检程序物理机安装

```bash
# 1. 安装Python
# 麒麟系统通常已预装Python 3
python3 --version

# 2. 安装依赖
pip3 install -r requirements.txt

# 3. 配置
cp config.example.yaml config.yaml
# 编辑config.yaml中的Prometheus和ES地址

# 4. 运行
python3 -m src.main inspect --skip-llm

# 5. 配置定时任务
crontab -e
# 添加: */30 * * * * /path/to/python3 -m src.main inspect --skip-llm >> /var/log/inspect.log 2>&1
```

### 6.7 物理机环境验证清单

| 组件 | 验证命令 | 预期输出 |
|------|----------|----------|
| Prometheus | `curl http://localhost:9090/-/healthy` | Prometheus is Healthy. |
| Node Exporter | `curl http://localhost:9100/metrics | head` | 指标数据 |
| Elasticsearch | `curl http://localhost:9200` | JSON响应含version |
| Grafana | `curl http://localhost:3000/api/health` | {"database":"ok"} |
| 巡检程序 | `python3 -m src.main inspect --skip-llm` | 生成报告 |

---

## 7. 配置详解

### 7.1 配置文件结构

```yaml
# config.yaml 完整配置示例

# Prometheus配置
prometheus:
  url: http://prometheus:9090    # Prometheus地址
  timeout_sec: 10               # 超时时间
  queries:                     # 监控指标列表
    - name: cpu_usage          # 指标名称
      promql: '...'           # PromQL查询语句
      threshold: 10           # 阈值
      unit: '%'               # 单位
      datacenter_filter: true  # 是否按机房过滤

# Elasticsearch配置
elasticsearch:
  url: http://elasticsearch:9200
  username_env: ES_USERNAME   # 用户名环境变量
  password_env: ES_PASSWORD    # 密码环境变量
  timeout_sec: 10
  queries:                    # 日志查询列表
    - name: error_logs_24h
      index: 'logstash-*'    # 索引名称
      query_string: 'level:ERROR OR level:FATAL'
      time_range_hours: 24  # 查询最近24小时
      size: 50               # 返回数量
      datacenter_filter: true
      ignore: false           # 是否忽略（不告警）
      alert: false            # 是否告警

# AI分析配置（可选）
llm:
  provider: anthropic
  model: claude-sonnet-4-6
  api_key_env: ANTHROPIC_API_KEY
  max_tokens: 2048
  enable_prompt_caching: true

# 报告配置
report:
  output_dir: reports
  language: zh-CN
  filename_format: '%Y-%m-%d-%H%M.md'
  datacenter_grouping: true
  include_summary: true
  include_recommendations: true

# 机房配置
datacenters:
  - name: 北京东坝
    code: dongba
    vip: 25.131.185.100
    components:
      - name: OMM
        count: 2
        ip_range: 25.131.185.181-182

# 告警配置
alerting:
  critical_alerts_zero: true   # 关键告警为0检查
  table_status_normal: true    # 表状态正常检查
  no_data_threshold: 0         # 无数据阈值
```

### 7.2 常用指标阈值

| 指标 | 阈值 | 说明 |
|------|------|------|
| cpu_usage | 10% | CPU使用率超过10%告警 |
| memory_usage | 60% | 内存使用率超过60%告警 |
| disk_usage | 80% | 磁盘使用率超过80%告警 |
| mysql_connections | 6000 | MySQL连接数超过6000告警 |
| mysql_qps | 2000 | QPS超过2000告警 |
| system_load | 32 | 系统负载超过32告警 |

### 7.3 环境变量

```bash
# 必需的环境变量
export ES_USERNAME=elastic
export ES_PASSWORD=your_password
export ANTHROPIC_API_KEY=sk-ant-xxx  # 可选，AI分析用

# 可选配置
export PROMETHUS_URL=http://prometheus:9090
export ES_URL=http://elasticsearch:9200
```

### 7.4 HTTPS配置

```yaml
# Nginx反向代理配置示例

server {
    listen 443 ssl;
    server_name prometheus.yourdomain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:9090;
    }
}
```

---

## 8. 常见问题排查

### 8.1 问题：Prometheus无法启动

**症状：** `docker-compose up` 失败，Prometheus容器报错

**排查步骤：**

```bash
# 1. 查看日志
docker logs prometheus

# 2. 常见错误：
# - 配置文件语法错误
# - 端口被占用
# - 权限问题

# 3. 验证配置
docker exec prometheus promtool check config /etc/prometheus/prometheus.yml
```

### 8.2 问题：无法采集到指标

**症状：** 巡检报告中显示"无数据"

**排查步骤：**

```bash
# 1. 检查Prometheus能否访问被监控目标
curl 'http://localhost:9090/api/v1/targets'

# 2. 检查Target状态是否为UP
# 在Prometheus Web界面查看Status -> Targets

# 3. 检查网络连通性
docker exec prometheus ping node-exporter-1
```

### 8.3 问题：Elasticsearch日志查询失败

**症状：** 巡检报告中ES部分显示错误

**排查步骤：**

```bash
# 1. 检查ES是否运行
curl http://localhost:9200

# 2. 检查索引是否存在
curl http://localhost:9200/_cat/indices

# 3. 检查日志格式是否正确
# 确认每条日志有 @timestamp, level, message 字段
```

### 8.4 问题：巡检程序运行报错

**症状：** `python -m src.main inspect` 报错

**排查步骤：**

```bash
# 1. 检查Python版本
python --version  # 必须是3.10+

# 2. 检查依赖
pip list | grep -E "httpx|pyyaml|click"

# 3. 检查配置文件
python -c "import yaml; yaml.safe_load(open('config.yaml'))"
```

### 8.5 问题：定时任务不执行

**症状：** 设定的时间点没有生成报告

**排查步骤：**

```bash
# Linux:
# 1. 检查cron服务状态
systemctl status crond

# 2. 查看日志
tail -f /var/log/cron

# 3. 测试手动执行
python -m src.main inspect --skip-llm
```

### 8.6 问题：GitHub推送失败

**症状：** `auto_push.py` 执行失败

**排查步骤：**

```bash
# 1. 检查Git配置
git config --list

# 2. 检查网络
ping github.com

# 3. 检查权限（需要GitHub Token）
# 在GitHub设置 -> Developer settings -> Personal access tokens
```

---

## 9. 维护与更新

### 9.1 日常维护

```bash
# 1. 每天检查
# - 查看巡检报告
# - 检查服务状态
docker-compose ps

# 2. 每周维护
# - 清理过期数据
# - 备份配置
# - 更新日志分析

# 3. 每月维护
# - 更新组件版本
# - 容量评估
# - 性能优化
```

### 9.2 数据清理

```bash
# Prometheus数据清理
docker exec prometheus promtool tsdb delete 2024-01-01

# ES索引清理（删除30天前的日志）
curl -X DELETE "http://localhost:9200/logstash-2024.01.*"
```

### 9.3 备份与恢复

```bash
# 备份配置文件
tar czf config-backup-$(date +%Y%m%d).tar.gz config.yaml prometheus/

# 恢复
tar xzf config-backup-20240502.tar.gz
```

### 9.4 版本更新

```bash
# 拉取最新代码
git pull origin master

# 重新构建
docker-compose build

# 重启服务
docker-compose down
docker-compose up -d
```

---

## 附录

### A. 命令速查表

| 命令 | 说明 |
|------|------|
| `docker-compose up -d` | 启动所有服务 |
| `docker-compose down` | 停止所有服务 |
| `docker-compose ps` | 查看服务状态 |
| `docker-compose logs -f` | 查看日志 |
| `python -m src.main inspect` | 运行巡检 |
| `python auto_push.py` | 推送GitHub |

### B. 端口速查表

| 端口 | 服务 | 说明 |
|------|------|------|
| 9090 | Prometheus | 指标存储 |
| 9200 | Elasticsearch | 日志存储 |
| 3000 | Grafana | 可视化 |
| 9100 | Node Exporter | 节点监控 |

### C. 巡检报告示例

```
# 巡检报告 — 2026-05-02 10:00 UTC

- 模式: 快照
- 机房数: 3
- 总异常指标: 2

---

## 各机房汇总

| 机房 | 异常指标 | 总指标 | 状态 |
|------|----------|--------|------|
| 北京东坝 | 1 | 11 | ⚠️ 有异常 |
| 北京南法信 | 1 | 11 | ⚠️ 有异常 |
| 合肥 | 0 | 11 | ✅ 正常 |

---

## 异常详情

### 北京东坝
- mysql_connections: 7200 > 6000

### 北京南法信
- instance_up: 0 < 1
```

---

**文档结束**

如有问题，请提交Issue: https://github.com/liuliu4356/kzx/issues