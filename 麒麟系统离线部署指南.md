# X 项目麒麟系统离线部署指南

> 目标：无外网环境下部署验证 X 项目

---

## 一、离线部署准备

在有外网的电脑上完成以下准备：

### 1.1 下载项目代码

```bash
# 克隆项目
git clone https://github.com/liuliu4356/kzx.git

# 打包项目
tar -czvf kzx.tar.gz kzx/
```

### 1.2 下载 Docker 镜像

```bash
# 拉取所有需要的镜像
docker pull prom/prometheus:v2.51.0
docker pull prom/alertmanager:v0.26.0
docker pull prom/node-exporter:v1.7.0
docker pull docker.elastic.co/elasticsearch/elasticsearch:8.13.0
docker pull docker.elastic.co/kibana/kibana:8.13.0
docker pull docker.elastic.co/logstash/logstash:8.13.0
docker pull docker.elastic.co/beats/filebeat:8.13.0
docker pull grafana/grafana:10.3.0
docker pull mysql:8.0
docker pull postgres:16-alpine
docker pull redis:7-alpine
docker pull nginx:alpine

# 导出镜像
docker save -o kzx-images.tar prom/prometheus:v2.51.0 prom/alertmanager:v0.26.0 prom/node-exporter:v1.7.0 docker.elastic.co/elasticsearch/elasticsearch:8.13.0 docker.elastic.co/kibana/kibana:8.13.0 docker.elastic.co/logstash/logstash:8.13.0 docker.elastic.co/beats/filebeat:8.13.0 grafana/grafana:10.3.0 mysql:8.0 postgres:16-alpine redis:7-alpine nginx:alpine
```

### 1.3 下载 Python 包

```bash
# 下载 Python 依赖包
pip download -r requirements.txt -d ./python-packages

# 打包
tar -czvf python-packages.tar.gz python-packages/
```

### 1.4 传输到麒麟系统

将以下文件拷贝到麒麟系统：
- `kzx.tar.gz` - 项目代码
- `kzx-images.tar` - Docker 镜像
- `python-packages.tar.gz` - Python 包（可选）

---

## 二、麒麟系统部署

### 2.1 导入 Docker 镜像

```bash
# 加载镜像
docker load -i kzx-images.tar

# 验证
docker images
```

### 2.2 解压项目代码

```bash
tar -xzvf kzx.tar.gz
cd kzx
```

### 2.3 安装 Python 依赖（离线）

```bash
# 方式1：使用预下载的包
pip install --no-index --find-links=./python-packages -r requirements.txt

# 方式2：如果有网络镜像，提前下载后拷贝
# 在有网络的机器上：pip download -r requirements.txt -d ./pkgs
# 拷贝到离线机器后：pip install --no-index --find-links=./pkgs -r requirements.txt
```

---

## 三、启动服务

### 3.1 方式一：Docker 启动（推荐）

```bash
cd ~/kzx

# 启动所有服务
docker-compose up -d

# 检查状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 3.2 方式二：Python 直接运行

```bash
cd ~/kzx

# 创建虚拟环境
python3 -m venv .venv

# 激活
source .venv/bin/activate

# 安装依赖
pip install --no-index --find-links=../python-packages -r requirements.txt

# 运行巡检（跳过 AI）
python -m src.main inspect --skip-llm
```

---

## 四、离线环境验证

### 4.1 验证服务启动

```bash
# 检查容器状态
docker-compose ps

# 预期输出：
#      Name              Command            State    Ports
# ----------------------------------------------------------------
# prometheus        /bin/prometheus ...   Up      9090:9090
# elasticsearch    /usr/local/bin ...  Up      9200:9200
# grafana          /run.sh            Up      3000:3000
# kibana           /usr/local/bin ...  Up      5601:5601
# ...
```

### 4.2 验证服务访问

```bash
# Prometheus
curl http://localhost:9090/api/v1/query?query=up

# Elasticsearch
curl http://localhost:9200

# Grafana（需认证）
curl -u admin:admin http://localhost:3000/api/health
```

### 4.3 验证巡检功能

```bash
cd ~/kzx
source .venv/bin/activate

# 运行巡检
python -m src.main inspect --skip-llm

# 检查报告
ls -la reports/
```

---

## 五、网络隔离配置

### 5.1 配置本地仓库（可选）

如果麒麟系统有本地仓库：

```bash
# 标记所有镜像
docker images --format "{{.Repository}}:{{.Tag}}" | while read img; do
  docker tag $img local-registry:5000/$img
  docker push local-registry:5000/$img
done
```

### 5.2 修改 docker-compose.yml

使用本地镜像：

```yaml
services:
  prometheus:
    image: prom/prometheus:v2.51.0  # 改为本地镜像
    # 或 image: local-registry:5000/prom/prometheus:v2.51.0
```

---

## 六、常见问题

### Q1：Docker 镜像加载失败

```bash
# 重新加载
docker load -i kzx-images.tar

# 检查镜像
docker images | grep -E "prometheus|elasticsearch|grafana"
```

### Q2：Python 包安装失败

```bash
# 检查包完整性
ls -la python-packages/

# 手动安装单个包
pip install ./python-packages/xxx.whl --no-index
```

### Q3：容器启动失败

```bash
# 查看具体错误
docker-compose logs 容器名

# 重建容器
docker-compose rebuild 容器名
```

---

## 七、一键部署脚本

创建 `deploy-offline.sh`：

```bash
#!/bin/bash
set -e

echo "========================================"
echo "X 项目离线部署脚本"
echo "========================================"

# 1. 导入镜像
echo "[1/5] 导入 Docker 镜像..."
if [ -f "kzx-images.tar" ]; then
  docker load -i kzx-images.tar
else
  echo "警告：未找到镜像文件，跳过"
fi

# 2. 解压项目
echo "[2/5] 解压项目代码..."
if [ -f "kzx.tar.gz" ]; then
  tar -xzvf kzx.tar.gz
fi

# 3. 安装 Python 依赖
echo "[3/5] 安装 Python 依赖..."
cd kzx
python3 -m venv .venv
source .venv/bin/activate

if [ -d "../python-packages" ]; then
  pip install --no-index --find-links=../python-packages -r requirements.txt
else
  pip install -r requirements.txt
fi

# 4. 启动服务
echo "[4/5] 启动 Docker 服务..."
docker-compose up -d

# 5. 验证
echo "[5/5] 验证服务..."
sleep 10
docker-compose ps

echo ""
echo "========================================"
echo "部署完成！"
echo "========================================"
echo "访问地址："
echo "  Prometheus: http://localhost:9090"
echo "  Elasticsearch: http://localhost:9200"
echo "  Grafana: http://localhost:3000 (admin/admin)"
```

执行：

```bash
chmod +x deploy-offline.sh
./deploy-offline.sh
```

---

## 八、快速验证

```bash
# 1. 检查所有服务
docker-compose ps

# 2. 运行巡检
cd kzx
source .venv/bin/activate
python -m src.main inspect --skip-llm

# 3. 查看报告
ls -la reports/
cat reports/$(ls -t reports/ | head -1)
```

---

**文档版本：** v1.0
**更新日期：** 2026-05-06