# X 项目测试环境搭建指南

> 此文档每 10 分钟自动更新 | 更新时间: 2026-05-02 16:11

---

## 一、项目概述

X 项目是一个**自动化监控巡检系统**，用于：
1. 监控服务器 CPU、内存、磁盘等指标（Prometheus + Node Exporter）
2. 收集分析系统日志（Elasticsearch + Logstash + Kibana）
3. 使用 AI 分析监控数据（Claude）
4. 自动生成巡检报告

---

## 二、服务状态检查

### 2.1 当前运行中的服务

| 服务 | 容器名 | 端口 | 状态 |
|------|--------|------|------|
| Prometheus | prometheus | 9090 | ✅ 正常运行 |
| Node Exporter | node-exporter | 9100 | ✅ 正常运行 |
| Node Exporter (多实例) | node-exporter-1/2/3 | 9101-9103 | ✅ 正常运行 |
| Alertmanager | alertmanager | 9093 | ✅ 正常运行 |
| Grafana | grafana | 3000 | ✅ 正常运行 |
| Elasticsearch | elasticsearch | 9200 | ✅ 正常运行 |
| Kibana | kibana | 5601 | ✅ 正常运行 |
| Logstash | logstash | 9600, 8080, 5044 | ✅ 正常运行 |
| Filebeat | filebeat | - | ⚠️ 重启中 |
| PostgreSQL | postgres-1 | 5432 | ✅ 正常运行 |
| MySQL | mysql-1 | 3306 | ✅ 正常运行 |
| Redis | redis-1 | 6379 | ✅ 正常运行 |
| Nginx | nginx-1 | 8081 | ✅ 正常运行 |
| postgres-exporter | postgres-exporter-1 | 9187 | ✅ 正常运行 |
| mysql-exporter | mysqld-exporter-1 | 9104 | ✅ 正常运行 |
| redis-exporter | redis-exporter-1 | 9121 | ✅ 正常运行 |
| nginx-exporter | nginx-exporter-1 | 9113 | ✅ 正常运行 |

---

## 三、Windows 本地搭建（详细步骤）

### 3.1 准备阶段

#### 步骤 1: 检查系统要求

**操作:**
1. 右键"此电脑" → 属性
2. 确认系统为 Windows 10/11 专业版/家庭版
3. 确认内存至少 8GB（推荐 16GB）
4. 确认硬盘剩余空间至少 50GB

#### 步骤 2: 安装 Docker Desktop

**操作:**
1. 访问 https://www.docker.com/products/docker-desktop/
2. 下载 Docker Desktop for Windows
3. 双击安装包 `Docker Desktop Installer.exe`
4. 勾选 "Use WSL 2 instead of Hyper-V"（推荐）
5. 点击 "Install" 等待安装完成
6. 安装完成后点击 "Close and restart"

**验证:**
```powershell
# 打开 PowerShell
docker --version
# 预期输出: Docker version 24.x.x

docker-compose --version
# 预期输出: docker-compose version v2.x.x
```

#### 步骤 3: 配置 Docker

**操作:**
1. 右键任务栏 Docker 图标 → Settings
2. 点击 "Resources" → "WSL INTEGRATION"
3. 勾选 "Enable integration with my WSL distro"
4. 点击 "Apply & Restart"

**设置内存限制:**
1. Settings → Resources → Memory
2. 建议设置为 4-8GB
3. 点击 "Apply"

---

### 3.2 项目部署阶段

#### 步骤 4: 克隆/下载项目

**操作:**
1. 如果有 Git:
```powershell
git clone <项目仓库地址> D:\claude_code开发\X
```

2. 如果手动下载:
- 解压到 `D:\claude_code开发\X` 目录

**验证:**
```powershell
ls D:\claude_code开发\X
# 应该看到 docker-compose.yml 等文件
```

#### 步骤 5: 安装 Python 环境

**操作:**
1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.10 或更高版本
3. 运行时:
   - ✅ 勾选 "Add Python to PATH"
   - ✅ 勾选 "Install pip"
   - 点击 "Install Now"

**创建虚拟环境:**
```powershell
cd D:\claude_code开发\X

# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
.venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

**验证:**
```powershell
.venv\Scripts\activate
python --version
pip list
# 应该看到 httpx, pyyaml, jinja2, anthropic, click, python-dotenv
```

#### 步骤 6: 配置环境变量

**操作:**
1. 打开记事本
2. 新建文件 `.env` 保存在 `D:\claude_code开发\X\` 目录
3. 添加内容:
```
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ES_USERNAME=elastic
ES_PASSWORD=your_password
```

**获取 Anthropic API Key:**
1. 访问 https://console.anthropic.com/
2. 登录后点击 "API Keys"
3. 点击 "Create API Key"
4. 复制密钥并填入 `.env` 文件

---

### 3.3 服务启动阶段

#### 步骤 7: 启动 Docker 服务

**操作:**
```powershell
cd D:\claude_code开发\X

# 方式1: 使用 start.bat
.\start.bat

# 方式2: 手动运行
docker compose up -d
```

**等待时间:** 约 1-2 分钟让所有服务完全启动

**验证服务状态:**
```powershell
docker ps
# 应该看到所有容器状态为 "Up"
```

#### 步骤 8: 验证各服务可访问

**操作:** 在浏览器中依次访问:

| 服务 | 地址 | 预期页面 |
|------|------|----------|
| Prometheus | http://localhost:9090 | Prometheus 控制台 |
| Grafana | http://localhost:3000 | Grafana 登录页 |
| Elasticsearch | http://localhost:9200 | JSON 状态信息 |
| Kibana | http://localhost:5601 | Kibana 登录页 |
| Node Exporter | http://localhost:9100 | 指标数据页面 |

**Grafana 登录:**
- 用户名: `admin`
- 密码: `admin`

---

### 3.4 运行巡检阶段

#### 步骤 9: 运行完整巡检

**操作:**
```powershell
cd D:\claude_code开发\X
.venv\Scripts\activate

# 完整巡检（带AI分析）
python -m src.main inspect

# 或跳过AI分析（更快）
python -m src.main inspect --skip-llm
```

#### 步骤 10: 查看巡检报告

**操作:**
```powershell
# 列出报告
dir D:\claude_code开发\X\reports

# 打开最新报告
notepad D:\claude_code开发\X\reports\2026-05-02-xxxx.md
```

---

## 四、Linux 服务器搭建（详细步骤）

以下步骤以 **Ubuntu 22.04 LTS** 为例，其他发行版（CentOS、Debian）操作类似。

### 4.1 系统准备阶段

#### 步骤 1: 更新系统

**操作:**
```bash
# 连接到服务器（使用 SSH）
ssh username@your-server-ip

# 更新软件包列表
sudo apt update && sudo apt upgrade -y

# 安装基础工具
sudo apt install -y curl wget git vim unzip
```

#### 步骤 2: 安装 Docker

**操作:**
```bash
# 安装依赖
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# 添加 Docker GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 添加 Docker 仓库
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 添加当前用户到 docker 组
sudo usermod -aG docker $USER
```

**验证:**
```bash
docker --version
docker-compose --version
```

#### 步骤 3: 安装 Docker Compose（独立版）

**操作:**
```bash
# 如果需要独立版 docker-compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

docker-compose --version
```

---

### 4.2 项目部署阶段

#### 步骤 4: 安装 Python

**操作:**
```bash
# Ubuntu
sudo apt install -y python3 python3-pip python3-venv

# 验证
python3 --version
```

#### 步骤 5: 克隆项目

**操作:**
```bash
# 创建项目目录
mkdir -p ~/x-project
cd ~/x-project

# 克隆项目（如果有仓库）
git clone <repository-url> .

# 或手动上传项目文件
# 使用 SFTP/FTP 将项目文件上传到服务器
```

**验证:**
```bash
ls -la ~/x-project
```

#### 步骤 6: 配置环境

**操作:**
```bash
cd ~/x-project

# 复制配置示例
cp config.example.yaml config.yaml
cp .env.example .env

# 编辑配置文件
nano config.yaml
```

**config.yaml 示例内容:**
```yaml
prometheus:
  url: http://localhost:9090
  timeout_sec: 10
  queries:
    - name: cpu_usage
      promql: '100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)'
      threshold: 80
      unit: '%'

llm:
  provider: anthropic
  model: claude-sonnet-4-6
  api_key_env: ANTHROPIC_API_KEY
```

**编辑 .env 文件:**
```bash
nano .env
```

添加:
```
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### 4.3 服务启动阶段

#### 步骤 7: 启动所有服务

**操作:**
```bash
cd ~/x-project

# 启动所有容器
sudo docker compose up -d

# 或使用旧版本命令
sudo docker-compose up -d
```

**等待:** 约 2-3 分钟让服务完全启动

**查看状态:**
```bash
docker compose ps
# 或
sudo docker ps
```

#### 步骤 8: 验证服务

**操作:**
```bash
# 使用 curl 测试各服务
curl http://localhost:9090
curl http://localhost:9200
curl http://localhost:5601
```

**在本地浏览器访问:**
```
http://你的服务器IP:9090    # Prometheus
http://你的服务器IP:3000    # Grafana
http://你的服务器IP:9200    # Elasticsearch
http://你的服务器IP:5601    # Kibana
```

**配置防火墙（如需要）:**
```bash
# Ubuntu (UFW)
sudo ufw allow 9090/tcp
sudo ufw allow 3000/tcp
sudo ufw allow 9200/tcp
sudo ufw allow 5601/tcp
sudo ufw enable
```

---

### 4.4 运行巡检阶段

#### 步骤 9: 创建 Python 虚拟环境

**操作:**
```bash
cd ~/x-project

# 创建虚拟环境
python3 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

#### 步骤 10: 运行巡检

**操作:**
```bash
cd ~/x-project
source .venv/bin/activate

# 运行巡检（跳过AI）
python -m src.main inspect --skip-llm

# 或完整巡检
python -m src.main inspect
```

#### 步骤 11: 查看报告

**操作:**
```bash
# 查看报告列表
ls -la ~/x-project/reports/

# 查看报告内容
cat ~/x-project/reports/2026-05-02-xxxx.md
```

---

### 4.5 远程访问配置（可选）

#### 配置 Nginx 反向代理

**操作:**
```bash
sudo apt install -y nginx

sudo nano /etc/nginx/sites-available/x-project
```

**添加配置:**
```nginx
server {
    listen 80;
    server_name x-project.yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /prometheus {
        proxy_pass http://localhost:9090;
    }

    location /elasticsearch {
        proxy_pass http://localhost:9200;
    }

    location /kibana {
        proxy_pass http://localhost:5601;
    }
}
```

**启用配置:**
```bash
sudo ln -s /etc/nginx/sites-available/x-project /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 五、Linux 云服务器（阿里云/腾讯云/华为云）

### 5.1 服务器购买与连接

#### 步骤 1: 购买云服务器

1. 登录云控制台（阿里云/腾讯云/华为云）
2. 购买 ECS 实例:
   - 系统: Ubuntu 22.04 LTS
   - 规格: 至少 2核4GB
   - 带宽: 5Mbps 以上
   - 开放端口: 80, 443, 9090, 3000, 9200, 5601

#### 步骤 2: 远程连接

**使用密码连接:**
```bash
ssh root@你的服务器IP
# 输入密码
```

**使用密钥连接:**
```bash
ssh -i your-key.pem root@你的服务器IP
```

### 5.2 初始化服务器

#### 步骤 3: 创建普通用户（安全）

```bash
# 创建用户
adduser xuser

# 添加到 sudo 组
usermod -aG sudo xuser

# 切换到该用户
su - xuser
```

#### 步骤 4: 配置_swap（避免内存不足）

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

## 六、进阶测试场景

### 6.1 测试监控指标采集

#### Prometheus 指标测试

**步骤:**
1. 访问 http://localhost:9090
2. 点击 "Graph" 标签
3. 在查询框输入以下查询:

| 查询 | 说明 |
|------|------|
| `up` | 查看所有采集目标状态 |
| `node_cpu_seconds_total` | CPU 使用时间 |
| `node_memory_MemTotal_bytes` | 内存总量 |
| `node_memory_MemAvailable_bytes` | 可用内存 |
| `node_filesystem_size_bytes` | 磁盘大小 |

4. 点击 "Execute" 执行查询

---

### 6.2 测试日志收集

#### 步骤 1: 生成测试日志

**使用 log-generator:**
```powershell
cd D:\claude_code开发\X
python log-generator.py
```

**或手动生成日志:**
```powershell
# Windows
echo "2026-05-02 ERROR Test error message" >> test.log

# Linux
echo "2026-05-02 ERROR Test error message" >> test.log
```

#### 步骤 2: 查看 Kibana 日志

1. 访问 http://localhost:5601
2. 点击 "Discover"
3. 选择索引模式 "logstash-*"
4. 搜索日志内容

---

### 6.3 测试告警功能

#### 配置 Alertmanager

**编辑 prometheus/alertmanager.yml:**
```yaml
global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  receiver: 'email'

receivers:
  - name: 'email'
    email_configs:
      - to: 'your-email@example.com'
        send_resolved: true
```

**重启 Alertmanager:**
```bash
docker compose restart alertmanager
```

---

## 七、常见问题 FAQ

### Q1: Docker 启动失败，提示 "Docker Desktop is not running"

**解决方法 (Windows):**
1. 点击任务栏的 Docker 图标
2. 选择 "Start Docker Desktop"
3. 等待显示 "Docker is running"
4. 重新运行 `docker compose up -d`

**解决方法 (Linux):**
```bash
sudo systemctl start docker
sudo systemctl enable docker
docker ps
```

---

### Q2: 容器一直 Restarting

**检查日志:**
```powershell
docker logs <容器名>
# 例如
docker logs filebeat
```

**常见原因:**
- 配置文件错误
- 端口被占用
- 权限问题

**解决方法:**
```powershell
# 重启单个容器
docker restart <容器名>

# 或重新创建
docker compose up -d --force-recreate <容器名>
```

---

### Q3: Prometheus 没数据

**检查 Targets:**

1. 访问 http://localhost:9090
2. 点击 menu → Status → Targets
3. 确认 all 状态为 "UP"

**手动添加数据:**

```powershell
# 测试 Node Exporter
curl http://localhost:9100/metrics

# 或 Linux
curl http://localhost:9100/metrics
```

---

### Q4: 报告生成失败，提示 "ANTHROPIC_API_KEY not set"

**解决方法 (Windows):**

```powershell
cd D:\claude_code开发\X
.venv\Scripts\activate
$env:ANTHROPIC_API_KEY="你的API密钥"
python -m src.main inspect
```

或在 `.env` 文件中添加:
```
ANTHROPIC_API_KEY=sk-ant-xxxxxxx
```

**解决方法 (Linux):**
```bash
cd ~/x-project
source .venv/bin/activate
export ANTHROPIC_API_KEY="你的API密钥"
python -m src.main inspect
```

---

### Q5: Grafana 登录不了

**重置密码:**

```bash
docker exec -it grafana grafana-cli admin reset-admin-password
```

**或删除数据重新创建:**
```bash
docker compose down
rm -rf grafana-data
docker compose up -d
```

---

### Q6: Elasticsearch 启动失败

**检查内存:**

```bash
docker stats
```

**常见原因:** 内存不足

**解决方法:**
1. 增加 Docker 内存分配
2. 或修改 `docker-compose.yml` 中的 ES 内存限制:
```yaml
- "ES_JAVA_OPTS=-Xms512m -Xmx512m"
```

---

### Q7: Filebeat 一直重启

**检查配置:**

```bash
docker logs filebeat
```

**常见原因:** filebeat.yml 配置错误

**解决方法:**
```bash
# 检查配置文件
docker exec -it filebeat cat /usr/share/filebeat/filebeat.yml

# 或重新创建
docker restart filebeat
```

---

### Q8: 端口被占用

**查找占用进程 (Windows):**

```powershell
netstat -ano | findstr "9090"
```

**查找占用进程 (Linux):**

```bash
sudo lsof -i :9090
sudo netstat -tlnp | grep 9090
```

**解决方法:**
1. 停止占用端口的程序
2. 或修改 docker-compose.yml 中的端口映射

---

### Q9: Kibana 无法连接 Elasticsearch

**检查:**

```bash
docker logs kibana
```

**解决方法:**

等待 1-2 分钟让 ES 完全启动，然后:
```bash
docker restart kibana
```

---

### Q10: 巡检报告中的数据为空

**检查 Prometheus 是否在采集:**

1. 访问 http://localhost:9090
2. 点击 "Graph"
3. 输入 `up` 点击执行

如果显示空结果，重启 node-exporter:
```bash
docker restart node-exporter
```

---

### Q11: Linux 服务器 SSH 连接失败

**检查 SSH 服务:**

```bash
sudo systemctl status ssh
sudo systemctl enable ssh
sudo systemctl start ssh
```

**检查防火墙:**

```bash
sudo ufw status
sudo ufw allow 22/tcp
```

---

### Q12: Docker 权限不足 (Linux)

**解决方法:**

```bash
# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER

# 重新登录
exit
ssh username@server
```

---

### Q13: 服务器内存不足

**解决方法 (Linux):**

```bash
# 查看内存使用
free -h

# 增加 swap
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

---

### Q14: 云服务器带宽不足

**解决方法:**
1. 升级云服务器带宽
2. 或使用 CDN 加速静态资源
3. 或只在内网访问

---

### Q15: 访问不到服务器端口

**检查防火墙 (Linux):**

```bash
sudo firewall-cmd --list-all
sudo firewall-cmd --add-port=9090/tcp --permanent
sudo firewall-cmd --reload
```

**检查云安全组:**
1. 登录云控制台
2. 找到安全组配置
3. 添加入站规则: TCP 端口 9090, 3000, 9200, 5601

---

## 八、一键测试脚本

### 8.1 Windows PowerShell 脚本

```powershell
# 保存为 test-all.ps1
# 使用方法: .\test-all.ps1

cd D:\claude_code开发\X

Write-Host "===== X 项目测试开始 =====" -ForegroundColor Green

# 1. 检查 Docker
Write-Host "`n[1/8] 检查 Docker..." -ForegroundColor Yellow
docker ps | Select-String "Up"

# 2. 检查端口
Write-Host "`n[2/8] 检查端口..." -ForegroundColor Yellow
$ports = @(9090, 3000, 9200, 5601, 9100, 5432, 6379, 3306)
foreach ($p in $ports) {
    $result = (Test-NetConnection -ComputerName localhost -Port $p -WarningAction SilentlyContinue).TcpTestSucceeded
    Write-Host "  端口 $p : $(if($result){'OK'}else{'失败'})"
}

# 3. 检查 Prometheus
Write-Host "`n[3/8] 检查 Prometheus..." -ForegroundColor Yellow
try {
    $r = Invoke-WebRequest -Uri http://localhost:9090 -UseBasicParsing -TimeoutSec 3
    Write-Host "  Prometheus: OK" -ForegroundColor Green
} catch {
    Write-Host "  Prometheus: 失败" -ForegroundColor Red
}

# 4. 检查 Elasticsearch
Write-Host "`n[4/8] 检查 Elasticsearch..." -ForegroundColor Yellow
try {
    $r = Invoke-WebRequest -Uri http://localhost:9200 -UseBasicParsing -TimeoutSec 3
    Write-Host "  Elasticsearch: OK" -ForegroundColor Green
} catch {
    Write-Host "  Elasticsearch: 失败" -ForegroundColor Red
}

# 5. 检查 Grafana
Write-Host "`n[5/8] 检查 Grafana..." -ForegroundColor Yellow
try {
    $r = Invoke-WebRequest -Uri http://localhost:3000 -UseBasicParsing -TimeoutSec 3
    Write-Host "  Grafana: OK" -ForegroundColor Green
} catch {
    Write-Host "  Grafana: 失败" -ForegroundColor Red
}

# 6. 检查 Python 环境
Write-Host "`n[6/8] 检查 Python 环境..." -ForegroundColor Yellow
.venv\Scripts\Activate
python --version

# 7. 运行巡检
Write-Host "`n[7/8] 运行巡检..." -ForegroundColor Yellow
python -m src.main inspect --skip-llm

# 8. 查看报告
Write-Host "`n[8/8] 巡检报告..." -ForegroundColor Yellow
Get-ChildItem reports\*.md | Sort-Object LastWriteTime -Descending | Select-Object -First 1

Write-Host "`n===== 测试完成 =====" -ForegroundColor Green
```

### 8.2 Linux Bash 脚本

```bash
#!/bin/bash
# 保存为 test-all.sh
# 使用方法: chmod +x test-all.sh && ./test-all.sh

cd ~/x-project

echo "===== X 项目测试开始 ====="

# 1. 检查 Docker
echo ""
echo "[1/8] 检查 Docker..."
docker ps | grep Up

# 2. 检查端口
echo ""
echo "[2/8] 检查端口..."
for port in 9090 3000 9200 5601 9100 5432 6379 3306; do
    if nc -z localhost $port 2>/dev/null; then
        echo "  端口 $port : OK"
    else
        echo "  端口 $port : 失败"
    fi
done

# 3. 检查 Prometheus
echo ""
echo "[3/8] 检查 Prometheus..."
curl -s http://localhost:9090 > /dev/null && echo "  Prometheus: OK" || echo "  Prometheus: 失败"

# 4. 检查 Elasticsearch
echo ""
echo "[4/8] 检查 Elasticsearch..."
curl -s http://localhost:9200 > /dev/null && echo "  Elasticsearch: OK" || echo "  Elasticsearch: 失败"

# 5. 检查 Grafana
echo ""
echo "[5/8] 检查 Grafana..."
curl -s http://localhost:3000 > /dev/null && echo "  Grafana: OK" || echo "  Grafana: 失败"

# 6. 检查 Python 环境
echo ""
echo "[6/8] 检查 Python 环境..."
source .venv/bin/activate
python --version

# 7. 运行巡检
echo ""
echo "[7/8] 运行巡检..."
python -m src.main inspect --skip-llm

# 8. 查看报告
echo ""
echo "[8/8] 巡检报告..."
ls -lt reports/*.md | head -1

echo ""
echo "===== 测试完成 ====="
```

---

## 九、服务健康检查命令汇总

### 9.1 Windows 命令

```powershell
# 快速检查所有服务状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 检查容器日志
docker logs -f <容器名>

# 重启单个服务
docker restart <容器名>

# 查看资源使用
docker stats

# 进入容器
docker exec -it <容器名> powershell
```

### 9.2 Linux 命令

```bash
# 快速检查所有服务状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 检查容器日志
docker logs -f <容器名>

# 重启单个服务
docker restart <容器名>

# 查看资源使用
docker stats

# 进入容器
docker exec -it <容器名> /bin/sh

# 查看系统资源
htop
df -h
free -h
```

---

## 十、紧急情况处理

### 10.1 完全重置 (Windows)

```powershell
cd D:\claude_code开发\X

# 停止所有服务
docker compose down

# 删除数据卷（如需要）
# Remove-Item -Recurse -Force .\prometheus-data
# Remove-Item -Recurse -Force .\grafana-data
# Remove-Item -Recurse -Force .\es-data

# 重新启动
docker compose up -d

# 等待 2 分钟让服务就绪
Start-Sleep -Seconds 120

# 检查状态
docker ps
```

### 10.2 完全重置 (Linux)

```bash
cd ~/x-project

# 停止所有服务
docker compose down

# 删除数据卷（如需要）
# rm -rf prometheus-data
# rm -rf grafana-data
# rm -rf es-data

# 重新启动
docker compose up -d

# 等待 2 分钟让服务就绪
sleep 120

# 检查状态
docker ps
```

---

## 十一、文档更新说明

此文档通过 Windows 任务计划每 10 分钟自动更新时间戳。

手动更新时间:
```powershell
python D:\claude_code开发\X\update_doc.py
```