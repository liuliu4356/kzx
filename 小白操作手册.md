# 🐣 小白操作手册 - X项目监控系统

> 📖 本手册面向零基础运维人员
> ⏱️ 预计完成时间：2小时
> 🎯 目标：独立完成系统搭建与验证

---

## 📋 目录

1. [第一章：准备工作](#第一章准备工作)
2. [第二章：快速搭建测试环境](#第二章快速搭建测试环境)
3. [第三章：验证系统功能](#第三章验证系统功能)
4. [第四章：配置生产环境](#第四章配置生产环境)
5. [第五章：日常使用](#第五章日常使用)
6. [附录：常见问题](#附录常见问题)

---

## 第一章：准备工作

### 1.1 需要准备的电脑配置

**最低配置要求：**
- 操作系统：Windows 10/11 或 Ubuntu 20.04+ 或 麒麟V10
- CPU：4核心
- 内存：8GB
- 硬盘：100GB可用空间
- 网络：能连接互联网（下载软件用）

> 💡 推荐使用Windows系统，因为本手册以Windows为主

### 1.2 需要安装的软件

| 软件 | 作用 | 安装难度 |
|------|------|----------|
| Docker Desktop | 容器管理 | ⭐⭐ |
| Git | 代码管理 | ⭐ |
| VS Code | 文本编辑器 | ⭐ |

### 1.3 安装 Docker Desktop（Windows版）

**步骤1：下载Docker**

1. 打开浏览器，访问：https://www.docker.com/products/docker-desktop/
2. 点击 "Download for Windows"
3. 下载 "Docker Desktop Installer.exe"

> ⚠️ 如果下载慢，可以直接用淘宝镜像等国内源

**步骤2：安装**

1. 双击下载的 `Docker Desktop Installer.exe`
2. 出现安装界面，点击 "Ok" 或 "Next"
3. 等待安装完成（约5分钟）
4. 安装完成后可能会提示"WSL 2 installation is required"，点击 "Ok" 自动安装

**步骤3：启动Docker**

1. 安装完成后，点击桌面上的 "Docker Desktop" 图标
2. 等待启动（首次启动约2分钟）
3. 看到鲸鱼图标，周围有很多小箱子，即启动成功

> 🔍 验证方法：打开命令提示符（CMD），输入：
> ```
> docker --version
> ```
> 如果显示版本号，说明安装成功

### 1.4 安装 Git（Windows版）

**步骤：**

1. 打开浏览器，访问：https://git-scm.com/download/win
2. 点击 "64-bit Git for Windows Setup"
3. 下载后双击安装，全部选"Next"即可
4. 安装完成后，在 CMD 中输入：
   ```
   git --version
   ```
   能显示版本即成功

### 1.5 安装 VS Code（Windows版）

**步骤：**

1. 打开浏览器，访问：https://code.visualstudio.com/
2. 点击 "Download for Windows"
3. 下载后安装，全部选"Next"即可

---

## 第二章：快速搭建测试环境

### 2.1 下载项目代码

**方法一：使用Git（推荐）**

1. 在电脑新建一个文件夹，命名为"项目"
2. 在文件夹空白处，鼠标右键，点击"Git Bash Here"
3. 弹出黑色窗口（终端），输入：
   ```
   git clone https://github.com/liuliu4356/kzx.git
   ```
4. 等待下载完成（约1分钟）

> 💡 如果Git clone失败，尝试：
> ```
> git clone https://ghproxy.com/https://github.com/liuliu4356/kzx.git
> ```

**方法二：直接下载**

1. 打开浏览器，访问：https://github.com/liuliu4356/kzx
2. 点击绿色按钮 "Code"
3. 点击 "Download ZIP"
4. 解压到"项目"文件夹

### 2.2 启动所有服务

**步骤：**

1. 打开命令提示符（CMD）
2. 进入项目目录：
   ```
   cd D:\项目\kzx
   ```
   > 💡 如果你的文件夹路径不是D盘，换成你的实际路径

3. 启动所有服务：
   ```
   docker-compose up -d
   ```
4. 等待启动（约3分钟）

**验证启动是否成功：**

```bash
docker-compose ps
```

如果看到类似这样的输出，说明成功：
```
     Name                    Command               State    Ports
---------------------------------------------------------------------------
prometheus          /bin/prometheus --config ...   Up      9090/tcp
elasticsearch       /usr/local/bin/docker-ent ...   Up      9200/tcp
grafana            /run.sh                         Up      3000/tcp
```

### 2.3 访问各个服务

**打开浏览器，在地址栏输入：**

| 服务 | 地址 | 用户名 | 密码 |
|------|------|--------|------|
| Grafana | http://localhost:3000 | admin | admin |
| Prometheus | http://localhost:9090 | (无需登录) | - |
| Elasticsearch | http://localhost:9200 | (无需登录) | - |

> 💡 建议把上面的地址保存到浏览器的收藏夹

---

## 第三章：验证系统功能

### 3.1 验证Prometheus是否正常工作

**操作步骤：**

1. 打开浏览器，访问 http://localhost:9090
2. 在页面中间的输入框输入：`up`
3. 点击 "Execute" 按钮
4. 下方应该显示类似这样的结果：
   ```
   up{job="prometheus",instance="localhost:9090"} 1
   ```

> ✅ 如果看到上述结果，说明Prometheus正常工作

### 3.2 验证Grafana是否正常工作

**操作步骤：**

1. 打开浏览器，访问 http://localhost:3000
2. 输入用户名：`admin`，密码：`admin`
3. 第一次登录会提示修改密码，可以跳过或改一个简单的
4. 进入主界面后，点击左侧菜单 "Dashboard"
5. 应该能看到已经导入的仪表板

> ✅ 能看到仪表板，说明Grafana正常工作

### 3.3 运行第一次巡检

**操作步骤：**

1. 打开命令提示符（CMD）
2. 进入项目目录：
   ```
   cd D:\项目\kzx
   ```
3. 运行巡检命令：
   ```
   docker run --rm --network kzx_x_default -v "%cd%":/app -w /app python:3.10-slim pip install -q -r requirements.txt && python -m src.main inspect --skip-llm
   ```
   > ⚠️ 第一次运行需要安装依赖，约2分钟

4. 运行完成后，查看报告：
   ```
   dir reports
   ```
5. 用记事本或VS Code打开最新的报告文件

> 💡 报告文件在 `reports` 文件夹中，文件名类似 `2026-05-02-xxxx.md`

### 3.4 验证异常检测功能

**操作步骤：**

巡检报告中，应该能看到类似这样的内容：

```
### Prometheus 指标

| 指标 | 值 | 阈值 | 状态 |
|---|---|---|---|
| cpu_usage | 5.2% | 10.0% | ✅ 正常 |
| instance_up | 1.0 | 1.0 | ✅ 正常 |
```

如果看到有"⚠️ 异常"的行，说明异常检测功能正常。

---

## 第四章：配置生产环境

> ⚠️ 如果只是测试环境，可以跳过本章，直接看第五章

### 4.1 生产环境与测试环境的区别

| 项目 | 测试环境 | 生产环境 |
|------|----------|----------|
| 访问地址 | localhost | 服务器IP |
| 数据存储 | 不持久化 | 需要持久化 |
| 资源限制 | 无限制 | 需要限制 |
| 安全性 | 低 | 高 |

### 4.2 获取服务器信息

在部署到生产环境前，需要向系统管理员获取以下信息：

| 信息 | 用途 | 示例 |
|------|------|------|
| 服务器IP地址 | 访问Prometheus/ES | 192.168.1.100 |
| 服务器SSH用户名 | 远程登录服务器 | root |
| 服务器SSH密码 | 远程登录服务器 | ******** |
| Elasticsearch密码 | 访问ES | ******** |

### 4.3 修改配置文件

**步骤：**

1. 用VS Code打开项目中的 `config.yaml` 文件
2. 找到以下行并修改：

```yaml
prometheus:
  # 把 localhost:9090 改成服务器IP地址
  url: http://192.168.1.100:9090

elasticsearch:
  # 把 localhost:9200 改成服务器IP地址
  url: http://192.168.1.100:9200
```

3. 保存文件

### 4.4 配置数据持久化

**步骤：**

1. 打开 `docker-compose.yml` 文件
2. 找到 prometheus 服务，修改为：

```yaml
services:
  prometheus:
    image: prom/prometheus:v2.51.0
    volumes:
      # 把本地文件夹映射到容器
      - ./prometheus-data:/prometheus
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
```

3. 同样方法配置 Elasticsearch

### 4.5 在服务器上部署

**步骤：**

1. 使用远程桌面或SSH连接到服务器
2. 在服务器上安装Docker（参考第一章）
3. 把项目文件上传到服务器（使用FTP或SCP）
4. 在服务器上运行：
   ```
   docker-compose up -d
   ```

### 4.6 验证生产部署

部署完成后，在本地电脑验证：

1. 打开浏览器，访问：`http://服务器IP:3000`
2. 打开浏览器，访问：`http://服务器IP:9090`
3. 运行巡检验证功能正常

---

## 第五章：日常使用

### 5.1 每天需要做什么

**1. 查看巡检报告**

```bash
# 进入项目目录
cd D:\项目\kzx

# 运行巡检
docker run --rm --network kzx_x_default -v "%cd%":/app -w /app python:3.10-slim pip install -q -r requirements.txt && python -m src.main inspect --skip-llm

# 查看最新报告
dir reports
```

**2. 登录Grafana查看仪表板**

- 打开 http://localhost:3000
- 查看Dashboard中的各项指标

**3. 检查Prometheus告警**

- 打开 http://localhost:9090
- 点击 "Alerts" 查看告警状态

### 5.2 每周需要做什么

1. 清理过期的巡检报告（删除30天前的）
2. 检查磁盘空间使用情况
3. 备份配置文件

### 5.3 每月需要做什么

1. 检查是否有软件更新
2. 查看系统容量是否需要扩容
3. 汇总巡检报告给领导

### 5.4 常见操作命令

| 命令 | 作用 |
|------|------|
| `docker-compose up -d` | 启动所有服务 |
| `docker-compose down` | 停止所有服务 |
| `docker-compose restart` | 重启所有服务 |
| `docker-compose logs -f` | 查看日志 |
| `docker-compose ps` | 查看服务状态 |

### 5.5 设置定时自动巡检（可选）

**Windows方法：**

1. 打开"任务计划程序"
2. 点击"创建基本任务"
3. 输入名称"自动巡检"
4. 触发器选择"每天"
5. 操作选择"启动程序"
6. 程序填入：
   ```
   cmd /c docker run --rm --network kzx_x_default -v "D:\项目\kzx":/app -w /app python:3.10-slim pip install -q -r requirements.txt && python -m src.main inspect --skip-llm
   ```

**Linux方法：**

```bash
crontab -e
# 添加下面这行（每天凌晨2点执行）
0 2 * * * /usr/bin/python3 /path/to/kzx/venv/bin/python -m src.main inspect --skip-llm
```

---

## 附录：常见问题

### Q1: Docker启动失败，提示"WSL 2 installation is required"

**解决方法：**
1. 打开Windows更新
2. 安装所有更新补丁
3. 重启电脑
4. 再次尝试启动Docker

### Q2: 启动docker-compose up -d 报错

**可能原因：** 端口被占用

**解决方法：**
```bash
# 查看哪些程序占用了端口
netstat -ano | findstr "9090"
# 结束占用端口的程序
taskkill /PID 进程ID /F
```

### Q3: 巡检报告生成失败

**可能原因：** 配置文件格式错误

**解决方法：**
1. 用VS Code打开 config.yaml
2. 检查是否有中文标点（应使用英文标点）
3. 检查缩进是否正确

### Q4: Elasticsearch无法访问

**可能原因：** 容器网络问题

**解决方法：**
```bash
# 重启Docker
docker-compose down
docker-compose up -d
```

### Q5: Grafana登录提示密码错误

**解决方法：**
1. 停止Grafana容器
2. 删除挂载的grafana-data文件夹（如果有）
3. 重新启动，Grafana会重置为admin/admin

### Q6: Prometheus无法采集到数据

**解决方法：**
1. 打开 http://localhost:9090
2. 点击 "Status" -> "Targets"
3. 查看是否有红色的Target（endpoint）
4. 检查网络连通性

### Q7: 报告中的指标显示"无数据"

**可能原因：** 指标名称不匹配

**解决方法：**
1. 在 http://localhost:9090 中手动查询指标
2. 确认指标名称与 config.yaml 中一致

### Q8: 如何更新到最新版本

```bash
# 进入项目目录
cd D:\项目\kzx

# 拉取最新代码
git pull origin master

# 重新构建
docker-compose build

# 重启服务
docker-compose down
docker-compose up -d
```

---

## 🎉 恭喜完成！

按照本手册操作，你应该已经：

- ✅ 安装了Docker、Git、VS Code
- ✅ 下载并启动了X项目监控系统
- ✅ 验证了Prometheus、Grafana、ES功能
- ✅ 运行了第一次巡检并查看了报告
- ✅ 了解了日常使用方法

如果遇到问题，请查阅上面的常见问题，或者在GitHub提交Issue：https://github.com/liuliu4356/kzx/issues

---

**手册版本：** v1.0
**更新日期：** 2026-05-02
**作者：** X项目开发组