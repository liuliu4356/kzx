filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/syslog
      - /var/log/messages
    fields:
      server: node-1
      role: web-server

output.logstash:
  hosts: ["host.docker.internal:5044"]