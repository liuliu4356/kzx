# X 项目麒麟系统快速验证指南

> 目标：在麒麟系统快速部署并验证功能是否正常

---

## 一、快速验证（推荐）

只需 3 步，5 分钟内完成验证。

### 步骤 1：下载项目

```bash
cd ~
git clone https://github.com/liuliu4356/kzx.git
cd kzx
```

### 步骤 2：启动本地服务

```bash
# 启动 Docker 环境
docker-compose up -d

# 等待 2 分钟，检查状态
docker-compose ps
```

### 步骤 3：运行巡检验证

```bash
# 使用 Docker 运行巡检（无需安装 Python）
docker run --rm --network kzx_default -v "$(pwd)":/app -w /app python:3.10-slim sh -c "pip install -q -r requirements.txt && python -m src.main inspect --skip-llm"
```

### 查看结果

```bash
# 查看是否生成报告
ls -la reports/

# 查看报告内容
cat reports/$(ls -t reports/ | head -1)
```

---

## 二、完整验证清单

如果第一步成功，继续验证以下功能：

### 2.1 Docker 服务验证

```bash
# 检查所有容器是否正常运行
docker-compose ps

# 预期输出：
# prometheus      Up    9090/tcp
# elasticsearch  Up    9200/tcp
# grafana       Up    3000/tcp
# kibana        Up    5601/tcp
```

### 2.2 Prometheus 验证

```bash
# 方式1：浏览器访问
http://localhost:9090

# 方式2：命令行验证
curl http://localhost:9090/api/v1/query?query=up

# 预期返回 JSON 数据
```

### 2.3 Elasticsearch 验证

```bash
curl http://localhost:9200

# 预期返回 JSON，包含 "cluster_name"
```

### 2.4 Grafana 验证

```bash
# 浏览器访问
http://localhost:3000

# 用户名：admin
# 密码：admin
```

### 2.5 巡检功能验证

```bash
# 生成测试日志
docker logs elasticsearch > /tmp/test.log 2>&1

# 运行巡检
docker run --rm --network kzx_default -v "$(pwd)":/app -w /app python:3.10-slim sh -c "pip install -q httpx pyyaml jinja2 click python-dotenv && python -m src.main inspect --skip-llm"

# 检查报告是否生成
ls -la reports/
```

---

## 三、常见问题排查

### 问题 1：docker-compose 启动失败

```bash
# 查看错误日志
docker-compose logs

# 检查端口占用
netstat -tlnp | grep -E "9090|9200|3000"
```

### 问题 2：巡检报告生成失败

```bash
# 检查 Prometheus 是否正常
docker exec prometheus curl localhost:9090

# 检查 Python 环境
docker run --rm -v "$(pwd)":/app python:3.10-slim python --version
```

### 问题 3：网络问题

```bash
# 检查容器网络
docker network ls

# 重建网络
docker-compose down
docker-compose up -d
```

---

## 四、一键验证脚本

创建 `verify.sh`：

```bash
#!/bin/bash
set -e

echo "========================================"
echo "X 项目快速验证脚本"
echo "========================================"

# 1. 下载/更新代码
echo "[1/5] 下载项目代码..."
cd ~/kzx || git clone https://github.com/liuliu4356/kzx.git ~/kzx
cd ~/kzx

# 2. 启动服务
echo "[2/5] 启动服务..."
docker-compose up -d

# 3. 等待服务启动
echo "[3/5] 等待服务启动（30秒）..."
sleep 30

# 4. 检查服务状态
echo "[4/5] 检查服务状态..."
docker-compose ps

# 5. 运行巡检
echo "[5/5] 运行巡检验证..."
docker run --rm --network kzx_default -v "$(pwd)":/app -w /app python:3.10-slim sh -c "pip install -q httpx pyyaml jinja2 click python-dotenv && python -m src.main inspect --skip-llm"

# 显示结果
echo ""
echo "========================================"
echo "验证完成！"
echo "========================================"
echo "报告位置："
ls -lt reports/ | head -3
```

执行验证：

```bash
chmod +x verify.sh
./verify.sh
```

---

## 五、验证结果判定

| 验证项 | 成功标志 |
|--------|----------|
| Docker 服务 | 所有容器状态为 Up |
| Prometheus | http://localhost:9090 可访问 |
| Elasticsearch | http://localhost:9200 可访问 |
| Grafana | http://localhost:3000 可登录 |
| 巡检报告 | reports/ 目录有 .md 文件 |

如果以上全部通过，说明项目功能正常，可以开始正式使用。

---

**文档版本：** v1.0
**更新日期：** 2026-05-06